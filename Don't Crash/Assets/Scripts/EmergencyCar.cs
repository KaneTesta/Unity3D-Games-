using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EmergencyCar : Car
{

    // Global car attributes
    private int speed = 2;
    private bool cantStop = true; // for cars that never can be stopped so player must prioritise getting them through


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
